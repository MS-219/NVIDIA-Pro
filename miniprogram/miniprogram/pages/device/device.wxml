<view class="container">
    <view class="custom-nav" style="padding-top: {{navBarTop}}px; height: {{navBarHeight}}px; line-height: {{navBarHeight}}px;">
        <text class="navbar-title">设备列表</text>
    </view>
    <!-- Search & Filter Bar -->
    <view class="search-bar-row">
        <view class="search-input-wrapper">
            <icon type="search" size="14" color="#999" style="margin-right: 6px;" />
            <input
                placeholder="搜索设备SN"
                placeholder-class="placeholder"
                class="search-input"
                value="{{searchKeyword}}"
                bindinput="onSearchInput"
                confirm-type="search"
                bindconfirm="handleSearch"
            />
        </view>
        <view class="search-btn" bindtap="handleSearch">搜索</view>
        <view class="filter-btn" bindtap="openFilterSheet">筛选</view>
        <view class="refresh-btn {{loading ? 'loading' : ''}}" bindtap="handleRefresh">↻</view>
    </view>
    <!-- Tabs -->
    <view class="status-tabs">
        <view class="tab {{currentTab === 'all' ? 'active' : ''}}" bindtap="switchTab" data-tab="all">
            全部 ({{allDeviceList.length}})
        </view>
        <view class="tab {{currentTab === 'online' ? 'active' : ''}}" bindtap="switchTab" data-tab="online">
            在线 ({{onlineCount}})
        </view>
        <view class="tab {{currentTab === 'offline' ? 'active' : ''}}" bindtap="switchTab" data-tab="offline">
            离线 ({{offlineCount}})
        </view>
    </view>
    <!-- Device List -->
    <view class="device-list">
        <block wx:if="{{deviceList.length > 0}}">
            <block wx:for="{{deviceList}}" wx:key="id">
                <view class="device-card-rich" bindtap="onManageDevice" data-id="{{item.id}}">
                    <!-- Top Row: SN & Status -->
                    <view class="card-row-top">
                        <view class="device-title-group">
                            <view class="device-title-line">
                                <text class="device-sn">{{item.bindCode || item.sn}}</text>
                                <view class="copy-tag" catchtap="onCopyContent" data-content="{{item.bindCode || item.sn}}">
                                    复制
                                </view>
                            </view>
                            <view class="card-row-sub">
                                <text class="biz-id">业务号: {{item.businessId}}</text>
                                <view class="copy-tag small" catchtap="onCopyContent" data-content="{{item.businessId}}">
                                    复制
                                </view>
                                <text class="carrier-tag">(移动)</text>
                            </view>
                        </view>
                        <view class="status-badge {{item.status}}">{{item.statusText}}</view>
                    </view>
                    <!-- Main Content -->
                    <view class="card-main-content">
                        <view class="thumb-wrapper">
                            <image class="device-thumb" src="/images/product.png" mode="aspectFit"></image>
                        </view>
                        <view class="device-center-info">
                            <view class="location-row">
                                <text class="icon-loc">⌖</text>
                                <text class="location-text">{{item.location}}</text>
                            </view>
                            <view class="remarks-row">
                                <text class="remarks-text">{{item.remarks}}</text>
                                <text class="icon-edit">✎</text>
                            </view>
                        </view>
                        <view class="device-right-info">
                            <view class="earnings-val">¥ {{item.earnings}}</view>
                            <view class="earnings-date">📅 {{item.date}}</view>
                        </view>
                    </view>
                    <!-- Footer Stats -->
                    <view class="card-footer-stats">
                        <view class="stat-col">
                            <text class="stat-label">可回收聚芯 Orin值</text>
                            <text class="stat-val">{{item.hashrate}}</text>
                        </view>
                        <view class="stat-divider"></view>
                        <view class="stat-col">
                            <text class="stat-label">上机时间</text>
                            <text class="stat-val">{{item.onlineTime}}</text>
                        </view>
                    </view>
                </view>
            </block>
        </block>
        <!-- Empty State -->
        <view class="empty-state" wx:else>
            <text class="empty-text">暂无设备</text>
        </view>
    </view>
</view>
